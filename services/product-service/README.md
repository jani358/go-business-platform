# Product Service

This service owns product domain logic and exposes CRUD APIs.

## Planned Endpoints

- `POST /products`
- `GET /products`
- `GET /products/{id}`
- `PUT /products/{id}`
- `DELETE /products/{id}`
